import 'package:patrol/patrol.dart';

// TODO: Replace with values specific to your app.

const patrolConfig = PatrolTesterConfig(appName: 'inzynierka');

const nativeAutomatorConfig = NativeAutomatorConfig(
  packageName: 'com.example.inzynierka',
  bundleId: 'com.example.inzynierka',
);
